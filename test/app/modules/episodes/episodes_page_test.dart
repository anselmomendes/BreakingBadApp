import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:BreakingBadApp/app/modules/episodes/episodes_page.dart';

void main() {
  testWidgets('EpisodesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EpisodesPage(title: 'Episodes')));
    //  final titleFinder = find.text('Episodes');
    //  expect(titleFinder, findsOneWidget);
  });
}
