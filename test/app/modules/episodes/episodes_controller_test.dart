import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:BreakingBadApp/app/modules/episodes/episodes_controller.dart';
import 'package:BreakingBadApp/app/modules/episodes/episodes_module.dart';

void main() {
  initModule(EpisodesModule());
  // EpisodesController episodes;
  //
  setUp(() {
    //     episodes = EpisodesModule.to.get<EpisodesController>();
  });

  group('EpisodesController Test', () {
    //   test("First Test", () {
    //     expect(episodes, isInstanceOf<EpisodesController>());
    //   });

    //   test("Set Value", () {
    //     expect(episodes.value, equals(0));
    //     episodes.increment();
    //     expect(episodes.value, equals(1));
    //   });
  });
}
