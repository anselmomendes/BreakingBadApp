import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:BreakingBadApp/app/modules/deaths/deaths_page.dart';

void main() {
  testWidgets('DeathsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DeathsPage(title: 'Deaths')));
    //  final titleFinder = find.text('Deaths');
    //  expect(titleFinder, findsOneWidget);
  });
}
