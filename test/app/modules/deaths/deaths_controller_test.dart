import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:BreakingBadApp/app/modules/deaths/deaths_controller.dart';
import 'package:BreakingBadApp/app/modules/deaths/deaths_module.dart';

void main() {
  initModule(DeathsModule());
  // DeathsController deaths;
  //
  setUp(() {
    //     deaths = DeathsModule.to.get<DeathsController>();
  });

  group('DeathsController Test', () {
    //   test("First Test", () {
    //     expect(deaths, isInstanceOf<DeathsController>());
    //   });

    //   test("Set Value", () {
    //     expect(deaths.value, equals(0));
    //     deaths.increment();
    //     expect(deaths.value, equals(1));
    //   });
  });
}
